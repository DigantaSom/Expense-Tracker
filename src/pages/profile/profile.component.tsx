import { ProfilePageContainer, Title } from './profile.styles';

const ProfilePage = () => {
  return (
    <ProfilePageContainer>
      <Title>Profile</Title>
    </ProfilePageContainer>
  );
};

export default ProfilePage;
